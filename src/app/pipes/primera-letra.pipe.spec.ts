import { PrimeraLetraPipe } from './primera-letra.pipe';

describe('PrimeraLetraPipe', () => {
  it('create an instance', () => {
    const pipe = new PrimeraLetraPipe();
    expect(pipe).toBeTruthy();
  });
});
